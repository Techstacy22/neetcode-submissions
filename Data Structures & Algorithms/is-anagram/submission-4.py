class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        # UPI 
        # U- if the two strings have same characters return true else return false
        # P- check the length. check if the sorted strings = each other

        # I
    
    
        if sorted(s) == sorted(t):
            return True
        return False


        